[project]
name = "eagle-search-indexer"
version = "0.1.0"
description = "Index Eagle.cool library with AI descriptions for semantic search"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "pillow>=12.0",
]
